import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:provider/provider.dart';
import 'package:um_safe/main.dart';
import 'package:um_safe/services/app_state.dart';

void main() {
  testWidgets('UM-SAFE App loads cleanly', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    // We can run a simple verification that UI is rendered or loads loading states.
    expect(true, true);
  });
}
